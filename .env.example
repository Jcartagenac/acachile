# ------------------------------------------------------------------------------
# ACA Chile Platform – Environment Template
# ------------------------------------------------------------------------------
# Copy this file to `.env.local` (root) to provide defaults for local tooling.
# Values prefixed with VITE_ are exposed to the frontend build. Backend secrets
# must be configured through Cloudflare Pages bindings for real deployments.
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# Runtime mode
# ------------------------------------------------------------------------------
NODE_ENV=development

# ------------------------------------------------------------------------------
# Frontend (Vite) variables
# ------------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:8787
VITE_ENVIRONMENT=development
FRONTEND_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173

# Optional: override default branding assets rendered in the header
VITE_HEADER_LOGO_URL=

# ------------------------------------------------------------------------------
# Cloudflare Pages Functions (Backend) – set via wrangler pages secret/var
# ------------------------------------------------------------------------------
ENVIRONMENT=development
JWT_SECRET=replace-with-dev-secret
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
FROM_EMAIL=noreply@example.com
ADMIN_EMAIL=admin@example.com
RESEND_API_KEY=
GOOGLE_MAPS_API_KEY=

# Object storage (R2) used by automation scripts
CLOUDFLARE_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=aca-chile-images
R2_PUBLIC_URL=

# ------------------------------------------------------------------------------
# Optional extras
# ------------------------------------------------------------------------------
# IMAGES asset binding fallback for local testing
IMAGES_BUCKET=
