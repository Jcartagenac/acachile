# ⚠️ IMPORTANTE: Este archivo .env.production NO se usa en producción
# 
# Las variables de entorno en producción DEBEN configurarse en:
# Cloudflare Pages Dashboard → Settings → Environment Variables
#
# Este archivo es solo como referencia para desarrollo local.
# Los valores aquí NO afectan la producción en Cloudflare Pages.
#
# Las variables deben configurarse vía wrangler CLI:
# npx wrangler pages secret put NOMBRE_VARIABLE --project-name=acachile --env=production
#
# O directamente en el Dashboard de Cloudflare Pages
#
# ============================================================================
# VARIABLES REQUERIDAS EN CLOUDFLARE PAGES (configurar en el Dashboard):
# ============================================================================
#
# Frontend (Variables Vite - se inyectan en el build):
# - VITE_API_BASE_URL=https://beta.acachile.com
# - VITE_ENVIRONMENT=production
# - VITE_DOMAIN=beta.acachile.com
# - VITE_R2_PUBLIC_URL=https://images.beta.acachile.com
#
# Backend (Pages Functions - disponibles como env.*):
# - FRONTEND_URL=https://beta.acachile.com
# - CORS_ORIGIN=https://beta.acachile.com
# - R2_PUBLIC_URL=https://images.beta.acachile.com
# - JWT_SECRET=your-secret-key
# - RESEND_API_KEY=re_xxx
# - GOOGLE_MAPS_API_KEY=AIza_xxx
#
# Para cambiar el dominio: actualiza las variables en Cloudflare Pages Dashboard
# y fuerza un nuevo deployment. NO es necesario modificar el código.
#
# ============================================================================

# Valores de fallback para desarrollo local (NO se usan en producción)
# Estas variables solo se usan si Vite no encuentra las variables de Cloudflare Pages
# El código tiene fallbacks inteligentes que usan window.location.origin en producción

# NOTA: Los valores a continuación son IGNORADOS en producción
# Solo se usan en desarrollo local si no hay un .env.local