{
  "name": "acachile-monorepo",
  "version": "1.0.0",
  "description": "ACA Chile - Asociación Chilena de Asadores - Monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "worker",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "build:worker": "npm run build --workspace=worker",
    "build:all": "npm run build:worker && npm run build",
    "preview": "npm run preview --workspace=frontend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=worker",
    "deploy": "npm run build:all && npm run deploy:worker && npm run deploy:frontend",
    "deploy:frontend": "npm run deploy --workspace=frontend",
    "deploy:worker": "npm run deploy --workspace=worker"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Jcartagenac/acachile.git"
  },
  "author": "ACA Chile",
  "license": "MIT"
}